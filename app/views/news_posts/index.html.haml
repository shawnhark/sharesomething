%article.news_posts_category
  - Category.all.each do |category|
    %header
      %h2= link_to category.name
    .news_posts.row
      - category.recent_news_posts.each do |news_posts|
        .news_post.col-md-2
          = link_to news_post_path(news_post.id), nil do